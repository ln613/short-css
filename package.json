{
  "name": "@ln613/css",
  "version": "1.1.2",
  "description": "A collection of shortcuts of common css rules",
  "main": "dist/out.css",
  "scripts": {
    "start": "watch \"npm run build\" src",
    "build": "node node_modules/jass src/in.jass dist/out.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nan Li",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "ramda": "^0.25.0",
    "watch": "^1.0.2"
  }
}
